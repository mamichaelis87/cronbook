<h2>Your Friends!</h2>
<ul>
<%# show all friendships, friend or pending %>
<% current_user.friendships.each do |friendship| %>
<li><%= friendship.friend.profile.first_name %> <%= friendship.friend.profile.last_name %>
<% if friendship.status == "pending" %>
    - PENDING 
  <% end %>
  <%= link_to "See Profile", user_path(friendship.friend) %>
</li>
<% end %>
</ul>
<h2>Find new friends</h2>
<ul>
<%# show all users.  
!need to find way to remove the friends/pending friends %>
  <% @users.each do |user| %>
  <li>
  <%= user.profile.first_name%> <%= user.profile.last_name%>
  <%= link_to "See Profile", user_path(user) %>
  </li>
  <% end %>
</ul>
<%= link_to "home", root_path%>